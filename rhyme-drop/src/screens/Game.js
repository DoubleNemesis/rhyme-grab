import styled from 'styled-components'

const GameContainer = styled.div`
color: whitesmoke;
`

export default function Game() {
    return (
        <GameContainer>
            Game
        </GameContainer>
    )
}